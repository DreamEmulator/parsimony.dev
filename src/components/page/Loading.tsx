import React from "react";
import { Box, Heading } from "@chakra-ui/react";
interface Props {}

const Loading: React.FC<Props> = ({}) => {
  return <Heading>Content loading...</Heading>;
};

export default Loading;
