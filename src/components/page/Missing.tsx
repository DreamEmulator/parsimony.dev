import React from "react";
import { Box, Heading } from "@chakra-ui/react";
interface Props {}

const Missing: React.FC<Props> = ({}) => {
  return <Heading>Content missing</Heading>;
};

export default Missing;
